server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://39.103.202.129:3306/graduation_design
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
  main:
    allow-circular-references: true

mybatis:
  #type-aliases-package: pri.tangjiang.graduationdesign.bean
  mapper-locations: classpath:mapper/*Mapper.xml
  check-config-location: true

logging:
  level:
    pri.tangjiang.graduationdesign.dao: debug

# pageHelper分页配置
pagehelper:
  helper-dialect: mysql
  reasonable: false
  support-methods-arguments: true
  params: count=countSql


